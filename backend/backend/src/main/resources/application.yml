spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/steves_custom_creations
    username: postgres
    password: "Jameson2021!"

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080